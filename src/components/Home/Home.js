import React from 'react';
import { Wrapper } from '../Home/HomeStyles';

const Home = ( { themeMode } ) => {
    return (
        <Wrapper
        themeMode = { themeMode }
        >

        </Wrapper>
    );
}

export default Home;